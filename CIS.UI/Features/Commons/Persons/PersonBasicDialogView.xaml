<local:DialogBase  x:Class="CIS.UI.Features.Commons.Persons.PersonBasicDialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:CIS.UI.Features"
        xmlns:persons="clr-namespace:CIS.UI.Features.Commons.Persons"
        local:DialogBase.DialogResult="{Binding Path=ActionResult}"
        Title="Person" SizeToContent="WidthAndHeight"
        MinHeight="0" MinWidth="0" >
    <StackPanel Style="{StaticResource ContentRoot}">
        <persons:PersonBasicView DataContext="{Binding Person}"/>

        <!-- buttons -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Ok" IsDefault="True" Command="{Binding Path=Accept}" Margin="4" />
            <Button Content="Cancel" IsCancel="True"  Margin="4" />
        </StackPanel>
    </StackPanel>
</local:DialogBase >
