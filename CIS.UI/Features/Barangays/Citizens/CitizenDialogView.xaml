<local:DialogBase x:Class="CIS.UI.Features.Barangays.Citizens.CitizenDialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:CIS.UI.Features"
        xmlns:citizens="clr-namespace:CIS.UI.Features.Barangays.Citizens"
        local:DialogBase.DialogResult="{Binding Path=ActionResult}"
        Title="CitizenDialogView" SizeToContent="WidthAndHeight">
    <StackPanel Style="{StaticResource ContentRoot}">
        <citizens:CitizenView DataContext="{Binding Citizen}"/>

        <!-- buttons -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save" IsDefault="True" Command="{Binding Path=Save}" Margin="4" />
            <Button Content="Cancel" IsCancel="True"  Margin="4" />
        </StackPanel>
    </StackPanel>
</local:DialogBase>
