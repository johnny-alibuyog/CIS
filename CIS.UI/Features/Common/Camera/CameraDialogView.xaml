<local:DialogBase x:Class="CIS.UI.Features.Common.Camera.CameraDialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:CIS.UI.Features"
        xmlns:camera="clr-namespace:CIS.UI.Features.Common.Camera"
        local:DialogBase.DialogResult="{Binding Path=ActionResult}"
        Title="CameraDialogView" SizeToContent="WidthAndHeight">
    <StackPanel Style="{StaticResource ContentRoot}">
        <camera:CameraView DataContext="{Binding Camera}" Width="600" />

        <!-- buttons -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Ok" IsDefault="True" Command="{Binding Path=Accept}" Margin="4" />
            <Button Content="Cancel" IsCancel="True"  Margin="4" />
        </StackPanel>
    </StackPanel>
</local:DialogBase>
