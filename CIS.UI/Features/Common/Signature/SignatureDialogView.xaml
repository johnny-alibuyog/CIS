<local:DialogBase x:Class="CIS.UI.Features.Common.Signature.SignatureDialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:CIS.UI.Features"
        xmlns:signature="clr-namespace:CIS.UI.Features.Common.Signature"
        local:DialogBase.DialogResult="{Binding Path=ActionResult}"
        Title="SignatureDialogView" SizeToContent="WidthAndHeight">
    <StackPanel Style="{StaticResource ContentRoot}">
        <signature:SignatureView DataContext="{Binding Signature}"/>

        <!-- buttons -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Ok" IsDefault="True" Command="{Binding Path=Accept}" Margin="4" />
            <Button Content="Cancel" IsCancel="True"  Margin="4" />
        </StackPanel>
    </StackPanel>
</local:DialogBase>
